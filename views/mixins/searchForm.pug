mixin searchForm(resource)
  form(action=`/${resouce}`, method="get")
    label(for="search") Search
    input(type="text" name="search" id="search" value= searchTerm)
    button(type="submit") Do Magic